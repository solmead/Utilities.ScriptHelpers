

export class Config {
    // Invert the Y axis so origin is at the bottom left
    public static readonly FLIP_Y_AXIS:boolean = false;

        // No implemented yet
    public static readonly  ENABLE_WEB_GL:boolean = false;

    public static readonly SHOW_REDRAW_REGIONS: boolean = false;

}